interface:
  display_name: "Image PPT To Editable PPT"
  short_description: "Convert image-only slides into editable PPTX files, module cutouts, or full-slide 4K image decks."
  brand_color: "#F0440D"
  default_prompt: "Use $image-ppt-to-editable-ppt to convert this image-only PPT into an editable deck, module-cutout deck, or full-slide 4K image-generated PPT while preserving source visuals."
policy:
  allow_implicit_invocation: true
